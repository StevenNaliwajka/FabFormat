def cf_find_edge_route(pt_one, pt_two, list_of_cf):
    # This method given a list of CF traces the edges from pt_one to pt_two and creates a matching
    #   list of primatives that traces the edge of the group.

    # Checks what cf pt_one is on.
    # then maps the connections on where each CF is touching each-other saves it to each CF.
    # then given that data start traveling between each CF creating a new CF.
    pass